const baseUrl = 'http://localhost';
const backendPort = ':8000';
const frontendPort = '';
const URL_FRONT = `${baseUrl}${frontendPort}`;
const URL_BACK = `${baseUrl}${backendPort}`;
export {URL_BACK, URL_FRONT}